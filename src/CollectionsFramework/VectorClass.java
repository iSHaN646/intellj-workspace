package CollectionsFramework;

import java.util.Vector;

public class VectorClass {

    public static void main(String[] args) {
        
        Vector<Integer> list = new Vector<>();

        // Same as ArrayList

        // Integer capacity() returns the current capacity of the vector

        // void setSize(int newSize) sets the size of vector
        // if newSize is greater, then null values are added at last
        // if newSize is lesser, index from size elements will be discarded

    
    }

    
    
}
